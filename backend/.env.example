# Port the API listens on.
PORT=3001

# Allowed CORS origin for the frontend. Use "*" for local dev.
CORS_ORIGIN=*

# PostgreSQL connection string (used from T06 onward for search history).
DATABASE_URL=postgres://satellitesnap:satellitesnap@localhost:5432/satellitesnap
