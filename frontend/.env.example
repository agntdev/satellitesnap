# Base URL of the SatelliteSnap backend API.
# Leave empty for the static GitHub Pages build — history then uses localStorage.
# Example for a deployed backend: https://api.satellitesnap.example
VITE_API_URL=
